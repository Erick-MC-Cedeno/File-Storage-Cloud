PORT=4000
NODE_ENV=development

# MongoDB connection string (keep your existing value)
MONGO_DB_URI=mongodb://127.0.0.1:27020/cloud-storage

# JWT secret (keep your existing value, rotate in production if needed)
JWT_SECRET=9Wlok16fqKsXqk8Y

# Client app origin for CORS
CLIENT_URL=https://refactored-fiesta-jj5q5599vxr3jj4p-3000.app.github.dev

# Rate limiting configuration
# Max requests per IP per window
RATE_LIMIT_MAX=50
# Window size in milliseconds
RATE_LIMIT_WINDOW_MS=60000